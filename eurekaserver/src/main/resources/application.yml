server:
  port: 8761
eureka:
  client:
    # 是否注册到服务中心
    register-with-eureka: false
    # 同步其他注册中心的注册信息
    fetch-registry: false
    # 注册地址
    service-url:
      defaultZone: http://localhost:8761/eureka/